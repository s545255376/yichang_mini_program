<view class="login">
    <image class="bk_img" src="https://yichangimg.luluhoo.cn/platform_pic/login_bk.jpg"></image>
    <view class="name">夷 畅 岩 茶 蒸 着 喝</view>

    <!-- <button class="login_btn" wx:if="{{btnType == 0}}" bindtap="getUserInfo">同意授权</button> -->
    <button class="login_btn" open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber" wx:if="{{btnType == 1}}">手 机 号 快 捷 登 录</button>
</view>


<!-- 协议 -->
<view wx:if="{{treatyshow == true}}">
    <view class="mask"></view>
    <view class="login_treaty">
        <view class="title">服务协议和隐私政策</view>
        <view class="info">
            感谢选择夷畅岩茶小程序，我们非常重视您的个人信息安全和隐私保护。根据最新法律要求，在使用本平台前，请仔细阅读并同意
            <view bindtap="goContract" data-id="{{item.id}}" wx:for="{{agreement}}" wx:key="agreementList">
                <text>《{{item.title}}》</text>
                {{(index != agreement.length-1)?'和':''}}
            </view>
            。请您充分了解在使用本平台过程中可能会收集、使用您的个人信息（如您的联系电话、位置等），我们也将采取网络安全保护等措施保障您的信息安全，确保您的个人权益！
        </view>
        <view class="btn">
            <view class="cancel" bindtap="cancelBtn">拒绝</view>
            <view class="confirm" bindtap="confirmBtn">同意并继续</view>
        </view>
    </view>
</view>

<pop bindeventclose="closeToast" popname="registerToast" wx:if="{{registerToast}}">
    <view id="pop_register_main">
        <view id="title">绑定邀请人</view>
        <view id="main">
            <van-field value="{{ registerValue }}" placeholder="请输入茶艺师手机号" border="{{ false }}" bind:change="registerOnChange" />
        </view>
        <view id="buttonGroup" bindtap="confirmBind">
            <view>确定</view>
        </view>
    </view>
</pop>